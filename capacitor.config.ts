import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.burjsoft.invitationmaker',
  appName: 'InvitationMaker',
  webDir: 'dist/invitation-fe/browser'
};

export default config;
